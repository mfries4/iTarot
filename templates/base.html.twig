<!DOCTYPE html>
<html lang="fr" data-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}iTarot{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🃏</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
        {% block stylesheets %}
        <style>
            :root[data-theme="light"] {
                --primary-warm: #e63946;
                --secondary-warm: #f77f00;
                --accent-warm: #fcbf49;
                --success-warm: #06a77d;
                --info-warm: #4ea8de;
                --bg-light: #fef9f3;
                --bg-card: #ffffff;
                --text-dark: #2b2d42;
                --text-muted: #6c757d;
                --border-color: #dee2e6;
                --navbar-bg: linear-gradient(135deg, #e63946 0%, #f77f00 100%);
                --footer-bg: linear-gradient(135deg, #2b2d42 0%, #4a4e69 100%);
                --shadow: rgba(0,0,0,0.1);
                --shadow-hover: rgba(0,0,0,0.15);
                --bs-body-bg: var(--bg-light);
                --bs-body-color: var(--text-dark);
                --bs-card-bg: var(--bg-card);
                --bs-card-color: var(--text-dark);
                --bs-heading-color: var(--text-dark);
                --bs-border-color: var(--border-color);
                --bs-table-bg: var(--bg-card);
                --bs-table-color: var(--text-dark);
                --bs-list-group-bg: var(--bg-card);
                --bs-list-group-color: var(--text-dark);
            }
            :root[data-theme="dark"] {
                --primary-warm: #ff6b7a;
                --secondary-warm: #ffa94d;
                --accent-warm: #ffd60a;
                --success-warm: #20c997;
                --info-warm: #66d9ef;
                --bg-light: #1a1a2e;
                --bg-card: #16213e;
                --text-dark: #eaeaea;
                --text-muted: #adb5bd;
                --border-color: #495057;
                --navbar-bg: linear-gradient(135deg, #c1121f 0%, #d65108 100%);
                --footer-bg: linear-gradient(135deg, #0f3460 0%, #16213e 100%);
                --shadow: rgba(0,0,0,0.3);
                --shadow-hover: rgba(0,0,0,0.5);
                --bs-body-bg: var(--bg-light);
                --bs-body-color: var(--text-dark);
                --bs-card-bg: var(--bg-card);
                --bs-card-color: var(--text-dark);
                --bs-heading-color: var(--text-dark);
                --bs-border-color: var(--border-color);
                --bs-table-bg: var(--bg-card);
                --bs-table-color: var(--text-dark);
                --bs-list-group-bg: var(--bg-card);
                --bs-list-group-color: var(--text-dark);
            }
            body {
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                background-color: var(--bg-light);
                color: var(--text-dark);
                font-size: 1.05rem;
                transition: background-color 0.3s ease, color 0.3s ease;
            }
            .navbar-brand {
                font-weight: bold;
                font-size: 1.8rem;
                letter-spacing: 1px;
            }
            main {
                flex: 1;
            }
            h1, h2, h3, h4, h5 {
                font-weight: 700;
                color: var(--text-dark);
            }
            h1 { font-size: 2.5rem; }
            h2 { font-size: 2rem; }
            h3 { font-size: 1.75rem; }
            h4 { font-size: 1.5rem; }
            .card {
                background-color: var(--bg-card);
                box-shadow: 0 4px 12px var(--shadow);
                transition: all 0.3s ease;
                border: 1px solid var(--border-color);
                border-radius: 12px;
            }
            .card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 20px var(--shadow-hover);
            }
            .card-header {
                border-radius: 12px 12px 0 0 !important;
                font-weight: 600;
                font-size: 1.2rem;
                border-bottom: 1px solid var(--border-color);
            }
            .stats-card {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                border-radius: 12px;
            }
            .stats-card .card-title {
                font-size: 1rem;
                opacity: 0.95;
                font-weight: 500;
            }
            .stats-card .display-6 {
                font-weight: bold;
            }
            .btn {
                font-weight: 600;
                border-radius: 8px;
                padding: 0.5rem 1.2rem;
                font-size: 1rem;
            }
            .btn-primary {
                background: linear-gradient(135deg, var(--primary-warm), var(--secondary-warm));
                border: none;
            }
            .btn-primary:hover {
                background: linear-gradient(135deg, #d62839, #e67100);
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(230, 57, 70, 0.3);
            }
            .btn-success {
                background-color: var(--success-warm);
                border: none;
            }
            .btn-success:hover {
                background-color: #058967;
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(6, 167, 125, 0.3);
            }
            .btn-info {
                background-color: var(--info-warm);
                border: none;
            }
            .alert {
                border-radius: 10px;
                border: none;
                font-size: 1.05rem;
            }
            .table {
                font-size: 1.05rem;
                color: var(--text-dark);
            }
            [data-theme="dark"] .table {
                --bs-table-bg: var(--bg-card);
                --bs-table-striped-bg: rgba(255,255,255,0.05);
            }
            .badge {
                font-size: 0.95rem;
                padding: 0.4rem 0.7rem;
                border-radius: 6px;
            }
            .text-muted {
                color: var(--text-muted) !important;
            }
            .form-control, .form-select {
                background-color: var(--bg-card);
                color: var(--text-dark);
                border-color: var(--border-color);
            }
            .form-control:focus, .form-select:focus {
                background-color: var(--bg-card);
                color: var(--text-dark);
                border-color: var(--primary-warm);
            }
            #theme-toggle {
                cursor: pointer;
                font-size: 1.3rem;
                padding: 0.4rem 0.8rem;
                background: rgba(255,255,255,0.2);
                border: none;
                border-radius: 8px;
                color: white;
                transition: all 0.3s ease;
            }
            #theme-toggle:hover {
                background: rgba(255,255,255,0.3);
                transform: scale(1.1);
            }
        </style>
        {% endblock %}
    </head>
    <body>
        {% if app.user %}
        <nav class="navbar navbar-expand-lg navbar-dark" style="background: var(--navbar-bg); box-shadow: 0 4px 12px var(--shadow);">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_home') }}" style="text-shadow: 2px 2px 4px rgba(0,0,0,0.2);">🃏 iTarot</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_home') }}" style="font-size: 1.1rem; font-weight: 500;">
                                <i class="bi bi-house-door-fill"></i> Accueil
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_list_index') }}" style="font-size: 1.1rem; font-weight: 500;">
                                <i class="bi bi-folder-fill"></i> Mes listes
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_player_index') }}" style="font-size: 1.1rem; font-weight: 500;">
                                <i class="bi bi-people-fill"></i> Joueurs
                            </a>
                        </li>
                    </ul>
                    <ul class="navbar-nav">
                        <li class="nav-item me-2">
                            <button id="theme-toggle" title="Changer de thème">
                                <i class="bi bi-moon-stars-fill" id="theme-icon"></i>
                            </button>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" style="font-size: 1.1rem; font-weight: 500;">
                                <i class="bi bi-person-circle"></i> {{ app.user.userIdentifier }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}" style="font-size: 1.05rem;">
                                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                                </a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        {% endif %}

        <main class="py-4">
            <div class="container">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endfor %}

                {% block body %}{% endblock %}
            </div>
        </main>

        <footer class="text-white text-center py-4 mt-5" style="background: var(--footer-bg);">
            <div class="container">
                <p class="mb-0" style="font-size: 1.05rem;">🃏 &copy; {{ 'now'|date('Y') }} iTarot - Gestion de parties de tarot avec passion</p>
            </div>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            // Gestion du thème dark/light
            const html = document.documentElement;
            const themeToggle = document.getElementById('theme-toggle');
            const themeIcon = document.getElementById('theme-icon');
            
            // Charger le thème sauvegardé
            const savedTheme = localStorage.getItem('theme') || 'light';
            html.setAttribute('data-theme', savedTheme);
            updateIcon(savedTheme);
            
            if (themeToggle) {
                themeToggle.addEventListener('click', () => {
                    const currentTheme = html.getAttribute('data-theme');
                    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
                    html.setAttribute('data-theme', newTheme);
                    localStorage.setItem('theme', newTheme);
                    updateIcon(newTheme);
                });
            }
            
            function updateIcon(theme) {
                if (themeIcon) {
                    themeIcon.className = theme === 'light' ? 'bi bi-moon-stars-fill' : 'bi bi-sun-fill';
                }
            }
        </script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
